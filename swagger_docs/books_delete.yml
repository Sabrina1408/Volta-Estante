tags:
  - Livros
summary: Remover livro (todas as cópias)
parameters:
  - name: ISBN
    in: path
    required: true
    type: string
responses:
  200:
    description: Livro removido com sucesso
    schema:
      type: object
      properties:
        message: { type: string }
        ISBN: { type: string }
  404:
    description: Livro não encontrado
